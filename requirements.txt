pandas>=2.0
numpy>=1.24
scipy>=1.10
requests>=2.31
pulp>=2.7
tabulate>=0.9
mcp[cli]>=1.0
pytest>=7.0   # for `make test`
# optional, only if the domains are reachable from your machine:
# soccerdata>=1.9    # Understat / FBref / Club Elo scrapers
# penaltyblog>=1.0   # alternative goal models
