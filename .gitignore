__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.DS_Store

# Rebuildable in ~30s via `make ingest`; 12 MB of binary that git cannot
# delta-compress. Never commit it — the CI workflow uploads it as an artifact
# and commits the projections CSV instead.
data/cache/
data/*.db
data/*.db-wal
data/*.db-shm
